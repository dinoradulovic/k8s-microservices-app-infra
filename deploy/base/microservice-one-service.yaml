---
apiVersion: v1
kind: Service
metadata:
  name: microservice-one-service-name
  labels:
    app: microservice-one-service-label
spec:
  type: NodePort
  ports:
  - port: 3000
    protocol: TCP
  selector:
    app: microservice-one-pod-label
